<div class="container-fiuld room">
    <h4 class="pb-4">検索結果:<%= @search_rooms.count %>件</h4>
    <div class="row">
        <div class="col-sm-6 col-lg-4 col-xl-3">
            <div class="room-frame-inner">
                <% @search_rooms.each do |room| %>
                <%= link_to  "/reservations/new/#{room.id}" do %>
                    <div class="room-image-out">
                        <%= image_tag "#{room.roomimage}", class: "room-image", alt: "部屋画像" %>
                    </div>
                        <div class="room-profile">
                        <p class="room-profile-name"><%= room.roomname %></p>
                        <p class="room-profile-city"><%= room.address %></p>
                        <p class="room-profile-price price-icon"><%= room.price %></p>
                    </div>
                <% end %>
                <% end %>
            </div>
        </div>
    </div>
</div>